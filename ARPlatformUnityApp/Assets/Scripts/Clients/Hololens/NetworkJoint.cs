using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NetworkJoint : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("NetworkJoint Start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
